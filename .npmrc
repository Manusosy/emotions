# Use legacy peer dependencies to avoid issues with npm 7+
legacy-peer-deps=true

# Use production environment on Vercel
production=true

# Cache npm dependencies for faster builds
cache=.npm

# Set registry to npm public registry
registry=https://registry.npmjs.org/ 